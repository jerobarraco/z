asm:
	extern	printf		; the C function, to be called
	SECTION .data		; Data section, initialized variables

var long a = 5
var str fmt = "a=%d, eax=%d\n"

fun int main():
	rax = a + 2
	printf(str fmt, int a, int rax)
